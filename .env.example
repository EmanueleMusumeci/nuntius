# Copy to .env and set real values. NEVER commit the real .env.
APP_ENV=dev

POSTGRES_DB=omiworld
POSTGRES_USER=omiworld
# Generate a strong one: openssl rand -hex 32
POSTGRES_PASSWORD=CHANGE_ME

# Caddy / public exposure
DOMAIN=omiworld.emanuelemusumeci.com
ACME_EMAIL=emanuele.musumeci@gmail.com
